import { TimerActivityDirective } from './timer.activity.directive';

describe('TimerActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new TimerActivityDirective();
    expect(directive).toBeTruthy();
  });
});

import { LinkActivityDirective } from './link.activity.directive';

describe('LinkActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkActivityDirective();
    expect(directive).toBeTruthy();
  });
});

import { FloatsDirective } from './floats.directive';

describe('FloatsDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatsDirective();
    expect(directive).toBeTruthy();
  });
});

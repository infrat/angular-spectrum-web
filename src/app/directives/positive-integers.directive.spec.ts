import { PositiveIntegersDirective } from './positive-integers.directive';

describe('PositiveIntegersDirective', () => {
  it('should create an instance', () => {
    const directive = new PositiveIntegersDirective();
    expect(directive).toBeTruthy();
  });
});
